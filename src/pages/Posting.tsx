import { PageHeader } from '@/components/PageHeader'
import { PostingDebugger } from '@/components/PostingDebugger'
import { MechanismBreakdown } from '@/components/MechanismBreakdown'
import { LmsBridge } from '@/components/LmsBridge'
import { CausalChain } from '@/components/CausalChain'
import { Callout, Card, CardHeader, CodeBlock, Collapse, Section, Table, Td, Th } from '@/components/ui'

const STEPS = [
  {
    no: 1,
    title: 'Завантаження документа',
    tech: 'Платформа отримує об’єкт документа: шапка + табличні частини. Відкривається транзакція.',
    fail: 'Документ помічений на видалення або не існує.',
  },
  {
    no: 2,
    title: 'Перевірка реквізитів',
    tech: 'ПроверитьЗаполнение() + власні перевірки модуля. Прапорець Отказ.',
    fail: 'Порожня стаття витрат: перевірки може не бути, і документ проведеться з порожньою аналітикою.',
  },
  {
    no: 3,
    title: 'Визначення алгоритму',
    tech: 'Виклик процедури, специфічної для виду документа. Логіка живе у конфігурації.',
    fail: 'Функціональна опція вимкнена — частина рухів не формується взагалі.',
  },
  {
    no: 4,
    title: 'Визначення рахунків',
    tech: 'Правила підстановки: за статтею витрат, за підрозділом, за видом номенклатури, за налаштуваннями обліку.',
    fail: 'Не налаштований рахунок обліку → або помилка, або підстановка «за замовчуванням» не туди.',
  },
  {
    no: 5,
    title: 'Визначення аналітики',
    tech: 'Заповнення субконто Дт/Кт і вимірювань регістрів значеннями з документа та довідників.',
    fail: 'Найчастіша тиха помилка: аналітика порожня, документ проведено, звіт нічого не показує.',
  },
  {
    no: 6,
    title: 'Формування рухів',
    tech: 'Створення наборів записів у ПАМ’ЯТІ. Тут їх ще можна змінити або скасувати.',
    fail: 'Нульова сума → рух формально є, але нічого не змінює.',
  },
  {
    no: 7,
    title: 'Запис рухів у регістри',
    tech: 'Движения.Записать() у транзакції. Старі рухи цього реєстратора видаляються, нові записуються.',
    fail: 'Закритий період, конфлікт блокувань, недостатньо прав → відкат усієї транзакції.',
  },
  {
    no: 8,
    title: 'Документ проведено',
    tech: 'Проведен = Истина. COMMIT. Дані стають видимими для звітів і для інших користувачів.',
    fail: '—',
  },
]

export default function Posting() {
  return (
    <div className="grid gap-6">
      <PageHeader
        slug="posting"
        lead="Проведення — єдиний місток між «даними про подію» і «фактом обліку». Усе, що відбувається в базі внаслідок вашої дії, відбувається саме тут. Нижче — покроковий debugger на реальних документах симулятора."
      />

      <PostingDebugger title="Debugger проведення" initialId="doc-rent" />

      <MechanismBreakdown
        title="Проведення: повний розбір"
        mechanism={{
          what:
            'Процедура, яка читає документ і формує з нього набори записів для регістрів, після чого записує їх у базу в межах однієї транзакції.\n\nЦе не «збереження документа» і не «розрахунок». Це ПЕРЕТВОРЕННЯ однієї форми даних (зручної людині) в іншу (придатну для агрегації).',
          why:
            'Бо форма документа і форма обліку принципово різні. У документі — одна послуга, одна сума, кілька реквізитів. В обліку той самий факт має бути поданий одночасно як: бухгалтерська кореспонденція, податковий факт, управлінська витрата і зміна заборгованості.\n\nНіхто не буде вводити це вручну чотири рази. Проведення робить це автоматично й узгоджено.',
          structure:
            'ОбработкаПроведения(Отказ, Режим) — точка входу.\nВсередині: очищення старих рухів → перевірки → читання потрібних даних з інших регістрів → формування рухів → запис.\n\nРежим буває оперативний (у реальному часі, з перевіркою залишків) і неоперативний (заднім числом, перевірки послаблені) — це важлива, але часто непомічена деталь.',
          who:
            'Користувач кнопкою «Провести», груповою обробкою, регламентним завданням або зовнішньою системою через API. Механізм однаковий у всіх випадках.',
          when:
            'У будь-який момент після запису документа. Повторне проведення (перепроведення) безпечне: платформа спочатку видаляє ВСІ рухи цього реєстратора, потім створює нові. Дублікатів не буде.',
          written:
            'Набори записів у всі регістри, які передбачив алгоритм. Зверніть увагу: записується не «різниця з попереднім проведенням», а повний набір заново.',
          used:
            'Після запису рухи стають видимими для звітів і для проведення інших документів. Саме тому порядок проведення має значення: документ реалізації, проведений раніше документа надходження, не побачить товару на складі.',
          ownSystem:
            'Одна сервісна функція post(documentId) з такими властивостями:\n\n1) ідемпотентна — повторний виклик дає той самий результат, а не дублює записи;\n2) транзакційна — усе або нічого;\n3) єдина точка створення ledger-записів — жоден інший код не має права писати в ledger;\n4) детермінована — з тих самих вхідних даних завжди ті самі записи.\n\nЦі чотири властивості дають вам 90% надійності 1С.',
          code: `Процедура ОбработкаПроведения(Отказ, Режим)
    // 1. знести свої попередні рухи
    Движения.Очистить();

    // 2. перевірки
    ПроверитьЗаполнение(Отказ);
    Если Отказ Тогда Возврат; КонецЕсли;

    // 3-5. алгоритм, рахунки, аналітика
    // 6. формування рухів у пам'яті
    СформироватьДвиженияПоУслугам();
    СформироватьДвиженияПоНДС();

    // 7. запис виконає платформа при COMMIT
    Движения.Хозрасчетный.Записывать = Истина;
КонецПроцедуры`,
          codeCaption: 'Скелет процедури проведення',
        }}
      />

      <Section eyebrow="вісім етапів" title="Що може піти не так на кожному кроці">
        <Table>
          <thead>
            <tr>
              <Th align="center">#</Th>
              <Th>Етап</Th>
              <Th>Що робить система</Th>
              <Th>Типова проблема</Th>
            </tr>
          </thead>
          <tbody>
            {STEPS.map((s) => (
              <tr key={s.no}>
                <Td align="center" mono className="font-semibold text-accent">{s.no}</Td>
                <Td className="font-medium">{s.title}</Td>
                <Td className="max-w-[360px] text-[12px] text-muted">{s.tech}</Td>
                <Td className="max-w-[320px] text-[12px] text-warn">{s.fail}</Td>
              </tr>
            ))}
          </tbody>
        </Table>
      </Section>

      <Callout kind="warn" title="Проведення читає, а не лише пише">
        Найнедооціненіша деталь. Документ реалізації під час проведення{' '}
        <strong>звертається до регістра залишків</strong>, щоб дізнатись собівартість і перевірити
        наявність товару. Це означає: результат проведення залежить від того,{' '}
        <em>що вже є в базі</em> на момент проведення. Саме звідси беруться класичні проблеми
        «перепровели документи в іншому порядку — змінилась собівартість».
      </Callout>

      <Collapse title="Що таке транзакція і чому це важливо саме тут" tone="accent">
        <p className="mb-2">
          Усе проведення виконується як одна операція бази даних. Якщо на сьомому кроці станеться
          помилка, шість попередніх теж відкотяться. Не буває «половини проведеного документа».
        </p>
        <CodeBlock
          lang="ts"
          caption="Та сама гарантія у власній системі"
          code={`await db.transaction(async (tx) => {
  await tx.delete(ledgerEntries).where(eq(ledgerEntries.sourceId, doc.id))  // ідемпотентність
  const entries = buildEntries(doc)                                          // чиста функція
  await tx.insert(ledgerEntries).values(entries)
  await tx.update(documents).set({ posted: true }).where(eq(documents.id, doc.id))
})
// помилка в будь-якому місці → ROLLBACK, база в узгодженому стані`}
        />
        <p className="mt-2">
          Зверніть увагу на перший рядок: <code>delete … where sourceId = doc.id</code>. Це прямий
          аналог <code>Движения.Очистить()</code> і саме він робить перепроведення безпечним.
        </p>
      </Collapse>

      <Card>
        <CardHeader
          title="Проведення vs Запис vs Розпроведення"
          subtitle="три різні операції, які легко переплутати"
        />
        <div className="p-4">
          <Table>
            <thead>
              <tr>
                <Th>Операція</Th>
                <Th>Що змінює</Th>
                <Th>Видно у звіті?</Th>
                <Th>Коли застосовується</Th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <Td className="font-medium">Записати</Td>
                <Td>Тільки дані документа</Td>
                <Td className="text-danger">ні</Td>
                <Td className="text-muted">Чернетка, документ ще уточнюється</Td>
              </tr>
              <tr>
                <Td className="font-medium">Провести</Td>
                <Td>Дані + рухи у регістрах</Td>
                <Td className="text-ok">так</Td>
                <Td className="text-muted">Операція підтверджена і має вплинути на облік</Td>
              </tr>
              <tr>
                <Td className="font-medium">Перепровести</Td>
                <Td>Видаляє старі рухи, створює нові</Td>
                <Td className="text-ok">так, оновлено</Td>
                <Td className="text-muted">Змінились дані документа або правила обліку</Td>
              </tr>
              <tr>
                <Td className="font-medium">Розпровести</Td>
                <Td>Видаляє всі рухи, дані лишаються</Td>
                <Td className="text-danger">ні</Td>
                <Td className="text-muted">Операція скасована, але документ треба зберегти</Td>
              </tr>
              <tr>
                <Td className="font-medium">Позначити на видалення</Td>
                <Td>Позначку; рухи вже мають бути видалені</Td>
                <Td className="text-danger">ні</Td>
                <Td className="text-muted">Документ введено помилково</Td>
              </tr>
            </tbody>
          </Table>
        </div>
      </Card>

      <CausalChain
        highlight={[2, 3, 4, 5, 6]}
        subtitle="Проведення — це етапи 3–7 наскрізної моделі"
        values={[
          'оренда',
          'ПНП-000001',
          'Провести',
          'СформироватьДвижения',
          'Дт 92 / Кт 631',
          'Госпрозрахунковий',
          'COMMIT',
          '',
          '',
          '',
        ]}
      />

      <LmsBridge
        summary="Правило, яке варто зробити залізним: ledger може змінювати ТІЛЬКИ функція post(). Ніяких прямих INSERT з контролерів, скриптів чи адмінки. Щойно з'явиться другий шлях запису — узгодженість зникне, і жоден звіт більше не можна буде довести."
        rows={[
          { onec: 'ОбработкаПроведения', lms: 'postDocument(id)', note: 'Єдина точка входу для створення записів.' },
          { onec: 'Движения.Очистить()', lms: 'DELETE WHERE source_id = :id', note: 'Робить повторний виклик безпечним.' },
          { onec: 'Отказ = Истина', lms: 'throw ValidationError', note: 'Виняток відкочує транзакцію цілком.' },
          { onec: 'Проведен = Истина', lms: 'documents.posted = true', note: 'Ознака, що записи існують.' },
          { onec: 'Оперативне проведення', lms: 'перевірка залишків перед записом', note: 'SELECT … FOR UPDATE, щоб уникнути гонок.' },
        ]}
        code={`export async function postDocument(docId: string) {
  return db.transaction(async (tx) => {
    const doc = await tx.query.documents.findFirst({ where: eq(documents.id, docId) })
    if (!doc) throw new NotFound(docId)

    validate(doc)                        // крок 2 — кидає ValidationError

    await tx.delete(ledgerEntries)       // крок 1 — ідемпотентність
      .where(eq(ledgerEntries.sourceId, docId))

    const entries = buildEntries(doc)    // кроки 3-6 — ЧИСТА функція, легко тестується
    if (entries.length) await tx.insert(ledgerEntries).values(entries)

    await tx.update(documents)           // крок 8
      .set({ posted: true, postedAt: new Date() })
      .where(eq(documents.id, docId))

    return entries
  })
}`}
        codeCaption="postDocument(): усі вісім кроків"
      />
    </div>
  )
}
